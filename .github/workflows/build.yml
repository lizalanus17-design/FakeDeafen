name: Build FakeDeafen
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: plugin

      - name: Clone plugins-template
        run: |
          git clone --depth=1 \
            https://github.com/Aliucord/plugins-template \
            template

      - name: Copy plugin
        run: |
          mkdir -p template/FakeDeafen/src/main/java/com/aliucord/plugins
          cp -r plugin/src/main/java/com/aliucord/plugins/FakeDeafen.java \
            template/FakeDeafen/src/main/java/com/aliucord/plugins/
          cp plugin/build.gradle.kts template/FakeDeafen/

      - name: Register plugin in settings
        run: |
          echo 'include(":FakeDeafen")' >> template/settings.gradle.kts

      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build
        working-directory: template
        run: ./gradlew :FakeDeafen:make --no-daemon

      - uses: actions/upload-artifact@v4
        with:
          name: FakeDeafen
          path: template/FakeDeafen/build/outputs/FakeDeafen.zip
          if-no-files-found: error
